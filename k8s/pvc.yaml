apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-stb-data
  namespace: mltru-com
spec:
  storageClassName: linode-block-storage-retain
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
